require "spec_helper"

describe Ibsenphrase do
  it "has a version number" do
    expect(Ibsenphrase::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
